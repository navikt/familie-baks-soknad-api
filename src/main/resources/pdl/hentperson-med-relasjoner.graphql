query ($ident: ID!) {
  person: hentPerson(ident: $ident) {
    navn {
      fornavn
      mellomnavn
      etternavn
    }
    familierelasjoner {
      relatertPersonsIdent
      relatertPersonsRolle
    }
  }
}
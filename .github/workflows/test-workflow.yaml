name: Test workflow
on:
  workflow_dispatch:

jobs:
  build:
    name: Build
    permissions:
      contents: read
      id-token: write
    uses: navikt/familie-baks-gha-workflows/.github/workflows/build-maven-app.yaml@byosbom-fiks
    with:
      skip-tests: true
      build-image: true
      push-image: true
      byosbom: target/classes/META-INF/sbom/application.cdx.json
    secrets: inherit
  deploy-with-new-image:
    name: Deploy with new image
    permissions:
      id-token: write
    needs: [build]
    uses: navikt/familie-baks-gha-workflows/.github/workflows/deploy.yaml@54c5d9228a37ca88ea58074a00eaa9cf9e90e7ec # ratchet:navikt/familie-baks-gha-workflows/.github/workflows/deploy.yaml@main
    with:
      image: ${{ needs.build.outputs.image }}
      cluster: dev-gcp
      resource: .nais/app-dev.yaml
    secrets: inherit
